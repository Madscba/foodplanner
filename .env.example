# =============================================================================
# Foodplanner - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values.
#
#   cp .env.example .env
#
# NEVER commit the .env file to version control.
# =============================================================================

# --- PostgreSQL ---------------------------------------------------------
POSTGRES_USER=foodplanner
POSTGRES_PASSWORD=           # REQUIRED - choose a strong password
POSTGRES_DB=foodplanner
POSTGRES_PORT=5432

# Async connection URL (used by FastAPI / SQLAlchemy)
# Constructed automatically in docker-compose; set manually for local dev:
# DATABASE_URL=postgresql+asyncpg://foodplanner:<password>@localhost:5432/foodplanner

# --- Neo4j Graph Database -----------------------------------------------
NEO4J_USER=neo4j
NEO4J_PASSWORD=              # REQUIRED - choose a strong password
NEO4J_URI=bolt://localhost:7687

# --- Redis ---------------------------------------------------------------
REDIS_URL=redis://localhost:6379/0

# --- pgAdmin (optional, for --profile tools) -----------------------------
PGADMIN_EMAIL=admin@foodplanner.local
PGADMIN_PASSWORD=            # REQUIRED if using pgAdmin

# --- External APIs -------------------------------------------------------
MEALDB_API_KEY=1             # TheMealDB free-tier test key

# LLM keys (optional - leave blank to disable LLM features)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Application ---------------------------------------------------------
ENVIRONMENT=development      # development | production
LOG_LEVEL=INFO               # DEBUG | INFO | WARNING | ERROR
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# --- Testing (only needed for running integration tests locally) ---------
# TEST_DATABASE_URL=postgresql://foodplanner:<password>@localhost:5432/foodplanner_test
