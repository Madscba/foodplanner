[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (require API key and/or database)
    slow: marks tests as slow (deselect with '-m "not slow"')
