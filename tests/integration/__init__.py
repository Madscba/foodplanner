"""Integration tests for foodplanner.

These tests require external dependencies:
- Network access for web scraping tests
- Database connection for E2E pipeline tests

Run with: pytest tests/integration/ -v
Skip with: pytest -m "not integration"
"""
